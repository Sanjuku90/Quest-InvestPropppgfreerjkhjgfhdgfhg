## Packages
framer-motion | For complex animations (roulette wheel, transitions)
lucide-react | Iconography
date-fns | Date formatting
clsx | Class name utility (likely already in base but good to ensure)
tailwind-merge | Class merging (likely already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
